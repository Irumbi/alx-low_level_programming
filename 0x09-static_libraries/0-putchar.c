#include "main.h"

/**
 *main -entry point print single character per
 *description -0-putchar
 *Return: 0
 */
int main(void)
{
	int ch;

	for (ch = "A"; ch <= "Z"; ch++
		_putchar(ch);
	return (0);
}
