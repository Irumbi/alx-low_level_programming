#include "main.h"
#include <unistd>
/**
 * _putchar - writes the character c to stdout
 * @c: The character to print
 *
 * Return: On sucess 1
 * On error, return -1
 */
int _putchar(char)
{
	return (write(1 &c, 1));
}
